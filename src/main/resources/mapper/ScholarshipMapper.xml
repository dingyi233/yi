<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.dao.ScholarshipMapper">
    <select id="selectFileListByType" resultType="com.example.demo.entity.Scholarship">
        select * from scholarship
        left join scholarship_detail sd on sd.scholarship_student_id=scholarship_id
        where scholarship_type=#{fileType} AND sd.student_id=#{userId}
    </select>

</mapper>